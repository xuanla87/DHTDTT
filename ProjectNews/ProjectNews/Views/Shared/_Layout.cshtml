<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    <link href="https://fonts.googleapis.com/css?family=Oswald:200,300,400,500,600,700&amp;subset=vietnamese" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Francois+One" rel="stylesheet">
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    <link href="~/css/font-awesome.min.css" rel="stylesheet" />
    <link href="~/Content/StyleSheetMobile.css" rel="stylesheet" />

</head>
<body>
    <div class="main-bannner">
        <div class="container">
            <div class="banner">
                @Html.Action("getMenuTop", "Home")
                @Html.Action("getBannerMain", "Home")
                @Html.Action("getSearch", "Home")
            </div>
        </div>
    </div>
    <div class="navbar navbar-inverse main-nav">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a href="/" class="navbar-brand home"><i class="fa fa-home"></i></a>
            </div>
            <div class="navbar-collapse collapse">
                @Html.Action("getMenuMain", "Home")
            </div>
        </div>
    </div>
    <div class="container body-content">
        @RenderBody()
        <footer>
            <div class="title text-center">
                Trường đại học thể dục thể thao bắc ninh
            </div>
            <div class="body">
                <div class="row">
                    @Html.Action("getBanDo", "Home")
                    <div class="col-lg-4 item  border-right">
                        @Html.Action("getFooterInfo", "Home")
                    </div>
                    <div class="col-lg-3 item  border-right">
                        @Html.Action("getAbout", "Home")
                    </div>
                    <div class="col-lg-2 item">
                        @Html.Action("_HitCounter", "Home")
                    </div>
                </div>
            </div>
        </footer>
    </div>
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
